# Used by the main chain (rag_chain/chain.py) — user-facing answer generation.
GROQ_API_KEY=

# Used by the RAGAS evaluator (eval/ragas_eval.py) — a different provider
# (Gemini) so evaluation runs draw from a separate free-tier quota and can
# never exhaust the quota your actual chat feature needs. Get a free key at
# https://aistudio.google.com/apikey
GOOGLE_API_KEY=
